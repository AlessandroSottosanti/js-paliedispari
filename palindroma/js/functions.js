
function reverseWords(word) {
    let reversedWord = '';
    for(i = word.length - 1; i >= 0; i-- ){
        reversedWord += word[i];
    }
    return reversedWord;
}

function palindromeCheck(word, invertedWord) {
    
}
